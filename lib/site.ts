export const SITE_URL = "https://socceradar.site";

/**
 * Core brand positioning — every piece of marketing copy (meta descriptions,
 * OG/Twitter tags, on-page taglines) should tie back to this and, where it
 * makes sense, to the /accuracy Track Record page: predictions checked
 * against what actually happened, not just confident-sounding claims.
 */
export const SITE_TAGLINE = "The prediction site that shows its work.";
